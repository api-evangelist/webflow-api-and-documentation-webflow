naftiko: 1.0.0-alpha2
info:
  label: Webflow Data API — Meta
  description: 'Webflow Data API — Meta. 2 operations. Lead operation: Webflow Get Authorization User Info. Self-contained
    Naftiko capability covering one Webflow Api And Documentation Webflow business surface.'
  tags:
  - Webflow Api And Documentation Webflow
  - Meta
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WEBFLOW_API_AND_DOCUMENTATION_WEBFLOW_API_KEY: WEBFLOW_API_AND_DOCUMENTATION_WEBFLOW_API_KEY
capability:
  consumes:
  - type: http
    namespace: webflow-data-meta
    baseUri: https://api.webflow.com/v2
    description: Webflow Data API — Meta business capability. Self-contained, no shared references.
    resources:
    - name: token-authorized_by
      path: /token/authorized_by
      operations:
      - name: authorizedby
        method: GET
        description: Webflow Get Authorization User Info
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: token-introspect
      path: /token/introspect
      operations:
      - name: introspect
        method: GET
        description: Webflow Get Authorization Info
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WEBFLOW_API_AND_DOCUMENTATION_WEBFLOW_API_KEY}}'
  exposes:
  - type: rest
    namespace: webflow-data-meta-rest
    port: 8080
    description: REST adapter for Webflow Data API — Meta. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/token/authorized-by
      name: token-authorized-by
      description: REST surface for token-authorized_by.
      operations:
      - method: GET
        name: authorizedby
        description: Webflow Get Authorization User Info
        call: webflow-data-meta.authorizedby
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/token/introspect
      name: token-introspect
      description: REST surface for token-introspect.
      operations:
      - method: GET
        name: introspect
        description: Webflow Get Authorization Info
        call: webflow-data-meta.introspect
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: webflow-data-meta-mcp
    port: 9090
    transport: http
    description: MCP adapter for Webflow Data API — Meta. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: webflow-get-authorization-user-info
      description: Webflow Get Authorization User Info
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: webflow-data-meta.authorizedby
      outputParameters:
      - type: object
        mapping: $.
    - name: webflow-get-authorization-info
      description: Webflow Get Authorization Info
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: webflow-data-meta.introspect
      outputParameters:
      - type: object
        mapping: $.
